<Application x:Class="UitgavesApp.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:UitgavesApp"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ImageBrush x:Key="WindowBackgroundImage" ImageSource="background.jpg"/>

        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource WindowBackgroundImage}"/>
        </Style>
        <Style TargetType="{x:Type local:MainWindow}" BasedOn="{StaticResource {x:Type Window}}"/>
    </Application.Resources>
</Application>
